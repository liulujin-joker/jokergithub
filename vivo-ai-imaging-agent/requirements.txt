# vivo AI 影像助手智能体 - 依赖
# 核心框架
pydantic>=2.0.0
typing-extensions>=4.8.0

# LLM 接口 (OpenAI 兼容协议 → DeepSeek / vivo 蓝心)
openai>=1.0.0
httpx>=0.25.0

# 图像处理
Pillow>=10.0.0
numpy>=1.24.0

# 向量存储 / 记忆
# chromadb (optional, not yet used)>=0.4.0
# sentence-transformers (optional, not yet used)>=2.2.0

# 工具 & 实用
tiktoken>=0.5.0
python-dotenv>=1.0.0
rich>=13.0.0

# 环境变量 (API keys)
python-dotenv>=1.0.0
